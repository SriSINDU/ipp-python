#include <string.h>
#include <stdio.h>
#include <stdlib.h>
 
int main() {
    char s[256];
    scanf("%[^\n]s",s);
    char* token = strtok(s, " ");
    while (token)
    {
    printf("%s\n", token);
    token = strtok(NULL, " ");
    }
   return 0;
}
